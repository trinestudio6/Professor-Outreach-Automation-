# Base URL of the n8n instance exposing workflows 02/05/06 (see ../workflows/README.md).
# Never commit the real value here, and never hardcode an owner_id/auth token in source —
# see the "API integration rules" section of CLAUDE.md.
VITE_API_BASE_URL=https://your-tunnel-or-domain.example/webhook
